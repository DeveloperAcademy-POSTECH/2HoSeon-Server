spring:
  application:
    name: twoHoSeon
  profiles:
    include: server, db, security, common, config
logging:
  level:
    root: info
    org.springframework.security.oauth2: debug
    com.twohoseon.app: debug
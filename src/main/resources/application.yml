spring:
  application:
    name: twoHoSeon
  profiles:
    active: local
    group:
      local:
        - dev
        - common
        - config
        - server
        - db
        - security
      prod:
        - prod
        - common
        - config
        - server
        - db
        - security

logging:
  level:
    root: info
    org.springframework.security: debug
    org.springframework.web: debug
    com.twohoseon.app: debug